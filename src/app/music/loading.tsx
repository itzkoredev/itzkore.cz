export default function LoadingMusic() {
  // Blank shell to immediately replace old tab without showing loading UI
  return <section className="container py-12"><div className="rounded-xl border border-transparent" /></section>;
}

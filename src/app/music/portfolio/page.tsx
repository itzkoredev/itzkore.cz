export const metadata = { title: 'Portfolio', description: 'Music portfolio', alternates: { canonical: '/music/portfolio' } };

import PortfolioClient from '../../../components/music/PortfolioClient';

export default function MusicPortfolioPage() {
  return <PortfolioClient />;
}
